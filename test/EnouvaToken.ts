import { expect } from "chai";

describe("EnouvaToken", function () {
  describe("Deployment", function () {
    it("Should set the right unlockTime", async function () {
      expect(1 + 1).to.equal(2);
    });
  });
});
